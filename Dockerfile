# Определение базового образа
FROM node:14

# Создание директории приложения
WORKDIR /usr/src/app

# Установка зависимостей
# Используется символ подстановки для копирования обоих файлов: package.json и package-lock.json
COPY package*.json ./

# Установка всех зависимостей
RUN npm install

# Копирование файлов исходного кода приложения
COPY . .

# Объявление порта, который будет прослушивать приложение
EXPOSE 3000

# Запуск приложения
CMD [ "node", "server.js" ]

